const detectiveImage = document.getElementById('detective');
const trueImage = document.getElementById('true');
const falseImage = document.getElementById('false');
const headerText = document.querySelectorAll('h1');
const inputBox = document.getElementById('text-input');
const submitBtn = document.getElementById('check-btn');

const isPalindrome = (userInput) => {
    // filter userInput (regex?)
    // check if palindrome
    // return true or false
}

const displayResult = (isPalindrome) => {
    //callback: isPalindrome
    //if true, display green elements
    //if false, display red elements

    if (isPalindrome) {

    } else {

    }
}